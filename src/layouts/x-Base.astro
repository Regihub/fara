---
import BottomNav from "../components/BottomNav.astro";
import DesktopNav from "../components/DesktopNav.astro";
import "../styles/global.css";

const { title = "Farnosť Stará Sásová" } = Astro.props;
---

<html lang="sk">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
  </head>

  <body class="m-0 bg-gray-100 text-gray-900">
    <!-- Horná navigácia iba pre desktop -->
    <DesktopNav />

    <!-- Hlavný obsah -->
    <div class="flex min-h-screen justify-center">
      <!--
        Mobil: max-w-md (ako doteraz)
        Desktop: max-w-5xl (primeraná šírka)
      -->
      <div class="w-full max-w-md lg:max-w-5xl bg-white">
        <!--
          Mobile: priestor pre spodné menu
          Desktop: bez spodného odsadenia
        -->
        <div class="pb-20 md:pb-0">
          <slot />
        </div>
      </div>
    </div>

    <!-- Spodná navigácia pre mobil -->
    <BottomNav />
  </body>
</html>
